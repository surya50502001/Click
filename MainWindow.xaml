<Window x:Class="HeyClicky.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HeyClicky" Height="200" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="What do you want HeyClicky to do?" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <TextBox x:Name="CommandTextBox" Grid.Row="1" Height="30" FontSize="14" Padding="5" Text="open blender"/>
        
        <Button x:Name="RunButton" Grid.Row="2" Content="Run Agent" Width="100" Height="35" HorizontalAlignment="Right" Margin="0,15,0,0" Click="RunButton_Click"/>
        
        <TextBlock x:Name="LogText" Grid.Row="2" Text="Ready." VerticalAlignment="Bottom" Foreground="Gray"/>
    </Grid>
</Window>
